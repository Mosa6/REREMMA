<% if post.bookmarked_by?(current_user) %>
  <%= link_to "ブックマークを外す", post_bookmarks_path(post), method: :delete, remote: true %>
<% else %>
  <%= link_to "ブックマーク", post_bookmarks_path(post), method: :post, remote: true %>
<% end %>
